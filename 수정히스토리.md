# 수정 히스토리

## 2026-07-05 JST
- (js/index.js, css/style.css) 티켓번호 컬럼 폭 축소: 110px → 96px (13px font 기준 10자리 "XAX2-XXXXX" ≈ 72px 텍스트 + 24px 패딩 = 96px 안전 여유 확보); 테이블 min-width 970→956px 연동
- (js/index.js) 원문 팝오버 클릭 시 닫기 추가: _origPopover에 click 리스너 추가, 텍스트 드래그 선택 중(window.getSelection().toString().length > 0)에는 닫기 스킵하여 복사 보호
- (js/index.js, css/style.css) 번역 'i' 아이콘 독립 컬럼 분리: 이슈명 셀 내부 flex 구조 제거, 티켓번호·이슈명 사이에 30px 고정폭 전용 컬럼(.orig-icon-col/.orig-icon-cell) 신설; hover 시 --info-i-bg(12% 투명도) 배경+border-radius:6px 하이라이트+transition 추가; tabindex="0"/aria-label="원문 보기"/Enter·Space 키보드 팝오버 오픈 키보드 접근성 추가; 번역 없는 행은 .orig-icon-empty 빈 td로 컬럼 폭 유지; navigate-cell에서 stopPropagation 제거(셀 분리로 이벤트 버블링 충돌 없음); min-width 940→970px 업데이트

## 2026-07-05 JST (이전)
- (js/index.js) 번역 'i' 아이콘 사전 안내 툴팁 위치 수정: rect.bottom+4(아이콘 아래=커서 위치) → rect.right+10 / rect.top-2(아이콘 오른쪽 옆+상단 정렬)로 변경, 커서와 겹침 해소; 화면 우측 벗어남 시 왼쪽 fallback 추가
- (js/index.js, css/style.css) 번역 'i' 아이콘 hover 사전 안내 툴팁 추가: PC hover 시 "원문 표시" 소형 툴팁 표시(11px, dark 배경, pointer-events:none); 클릭 시 툴팁 즉시 닫고 원문 팝오버 표시; _showPreviewTooltip/_hidePreviewTooltip 함수 추가, _toggleOrigPopover에 _hidePreviewTooltip 호출 삽입; 터치 환경(mouseover 미발동)에서는 자동 미작동
- (css/style.css) 번역 'i' 아이콘 hit area 내 왼쪽 정렬: padding 5px 14px → 5px 23px 5px 5px; 'i' 위치=5px(td left border 기준), 히트박스 전체 너비(33px) 및 위치 불변, 우측 여백 23px 확보
- (css/style.css) 번역 'i' 아이콘 hit area 내 좌우 중앙 정렬: padding 5px 8px 5px 20px → 5px 14px(대칭)으로 변경; 'i' 위치=14px(td left border 기준), 좌우 각 14px 균등; 우측 hit area 8px→14px 확대; 모바일 padding-left override 제거(base 14px 대칭 통일)
- (css/style.css) 번역 'i' 아이콘 클릭 영역 확대: margin-left:-12px + padding-left:20px 조합으로 이슈명 셀 td 왼쪽 padding 전체(12px)를 hit area에 포함, 'i' 글자 시각적 위치 유지; .title-cell-inner overflow:hidden 제거(음수 margin 영역 clip 방지); 768px 미만 8px 기준 media query 추가
- (js/index.js, css/style.css) 번역 'i' 아이콘 클릭 시 상세 이동 버그 수정:
  - 팝오버 open 로직을 document 레벨에서 td 레벨로 이동, 아이콘 감지 시 e.stopPropagation() 호출로 navigate-cell 전파 완전 차단
  - _toggleOrigPopover() 함수 분리(토글 로직 공유), document 리스너는 외부 클릭 시 닫기만 담당
  - 클릭 영역 확대: padding:5px 8px 추가(hit area ~21×23px), display:inline-flex;align-items:center 추가
- (js/index.js, css/style.css) 번역 안내 'i' 아이콘 리디자인: 인디고 원형 ⓘ 삭제, Georgia 세리프 이탤릭 italic 'i' 단독 표시(안내데스크 스타일); 색상 --info-i(cyan-600) CSS 변수화, 다크모드 지원; 아이콘 위치를 이슈명 텍스트 앞으로 이동; hover 시 밑줄 나타나는 인터랙션 추가
- (Code.gs) ⓘ 아이콘 미표시·번역 로직 슬래시 순서 버그 수정: buildTranslatedTitle 정규식 일본어 run 감지 방식으로 전면 교체(영어 먼저/슬래시 복수 케이스 처리); backfillTitleTranslationsForce() 추가(기존 캐시 무시 전체 강제 재번역)
- (js/index.js, css/style.css) title-cell overflow:hidden이 ⓘ 아이콘을 가리는 문제 수정: title-cell-inner flex 래퍼+title-text span 구조로 변경
- (Code.gs, js/index.js, js/detail.js, detail.html, css/style.css) 이슈명 다국어 번역 캐싱 기능 추가:
  - Code.gs: COL.TITLE_KO(R열), COL.TITLE_VI(S열) 신규 컬럼 추가; containsJapanese/splitTitleParts/buildTranslatedTitle 헬퍼 함수; addTicket/updateTicket 저장 시 LanguageApp.translate()로 KO·VI 번역 캐싱; rowToObj에 title_ko/title_vi 포함; backfillTitleTranslations() 일회성 백필 함수 추가
  - index.js: 언어모드(KO→title_ko, VI→title_vi, JP·EN→원문)에 따라 buildRow 이슈명 셀 표시 전환; 번역 시 ⓘ 아이콘 표시; setupOrigTitlePopover() — 클릭/탭으로 원문 팝오버 표시(모바일 호환), 다른 곳 클릭 시 닫힘
  - detail.js: fillForm 후 updateTitleTranslationHint() 호출 — KO/VI 모드에서 번역된 이슈명 힌트 표시
  - detail.html: #title-translation-hint div 추가
  - style.css: .title-orig-icon(인디고 원형 ⓘ), .orig-title-popover(다크 팝오버), .title-translation-hint/.title-hint-label/.title-hint-text 스타일 추가

## 2026-07-04 JST
- (js/api.js, js/index.js) 에러 처리 개선 + 자동 재시도: getTickets() 에러를 네트워크 오류/HTTP 오류/JSON 파싱 오류 3가지로 구분하고 console.error로 상세 기록; loadTickets()에 1회 자동 재시도(1.5s 대기) 추가 — 1차 실패 시 배너 없이 재시도, 재시도도 실패할 때만 에러 배너 표시, 로딩 인디케이터는 재시도 동안 유지
- (detail.html, css/style.css) 이슈명 입력란 `<input>` → `<textarea rows="3">`로 교체: 긴 제목이 잘리지 않고 3줄 높이 고정 표시, resize:vertical 추가, .input-clearable align-items:flex-start + textarea 공유 스타일, .btn-clear-input min-height:36px

## 2026-07-04 JST
- (css/style.css, js/index.js) 버그수정: 스크롤 힌트 버튼 클릭 가로채기(주범) + setupDragDrop 중복 등록 수정 (commit c22f8f4)
  - [버그B] .scroll-hint-btn pointer-events를 기본 none으로 변경, .visible 상태일 때만 auto 전환 — 오버레이 비가시 상태(opacity:0)에서도 버튼이 pointer-events:auto로 클릭을 소리없이 가로채, 테이블 스크롤 후 이슈명 셀이 버튼 위치와 겹칠 때 엉뚱한 티켓으로 이동하던 버그 해소
  - [버그A] renderSection 내 setupDragDrop 호출 제거 — tbody.innerHTML은 <tr>만 교체하고 tbody 자체는 유지되므로 DOMContentLoaded 호출 한 번으로 충분, 자동갱신 때마다 drag 리스너가 누적 등록되던 문제 해소
- (css/style.css, js/index.js) 하단 sticky 스크롤바에 ◀▶ 화살표 버튼 추가 [별도 커밋 — git revert로 단독 제거 가능]: 클릭=25% smooth scroll, hold=400ms 후 10px×30ms 반복, 끝 도달 시 해당 방향 버튼 비활성(opacity 0.3), 트랙 flex:1 배치로 전체 폭 자동 조정
- (css/style.css, js/index.js) 좌우 스크롤 힌트 버튼 추가 [실험적 기능 — 필요시 git revert로 단일 커밋 되돌리기 가능]: 테이블 좌/우 끝에 그라데이션 오버레이 + 동그란 화살표 버튼, 스크롤 위치 따라 동적 표시/숨김, 클릭 시 컨테이너 폭 75% smooth scroll
- (css/style.css, js/index.js) 테이블 가로스크롤바 재구성: 상단 커스텀 스크롤바 제거 → 브라우저 기본 스크롤바 시각적으로 숨김(scrollbar-width:none) → `position:sticky; bottom:0` 하단 고정 커스텀 스크롤바 신규 추가, `.ticket-section { overflow:clip }` 으로 변경(hidden→sticky 오작동 방지)
- (CLAUDE.md) 파일 내 수정이력 주석 금지 규칙 추가 — 커밋 메시지로 대체
- (Code.gs, css/style.css, js/index.js, js/detail.js, js/api.js, js/i18n.js, js/versions.js, detail.html, index.html, versions.html) 기존 수정이력 주석 12줄 일괄 제거

## 2026-07-03 JST
- (index/detail/versions.html, favicon_32x32.png) 파비콘을 마루시스 공식 favicon_32x32.png로 교체, 구 marusys-ci.png 삭제 (commit 33508e2, a365c42, 5c440d9)
- (js/index.js) 실시순서 정렬: 빈칸끼리는 티켓번호 오름차순 2차 정렬(sortByPriority + ticketNo 헬퍼) (commit 8c58a25)
- (js/index.js) 드래그 규칙 4케이스 재작성(dragend): 드롭 위치의 번호영역/빈칸영역 판정 → 번호영역 진입 시 1..n 재번호(빈칸→번호 승격 포함), 빈칸영역 진입 시 드래그 항목만 번호 삭제하고 gap 유지(번호→빈칸), 빈칸→빈칸은 변화 없음 (commit 8c58a25)
- (js/index.js, css/style.css) 헤더 필터 표시를 Google식 아이콘 교체로 변경: 하단 뱃지(값+×) 제거로 UI 밀림 해소 → 비활성=얇은 ▼, 활성=깔때기 아이콘+인디고 강조(--filter-active)+pill 배경, 값은 select title 툴팁, 해제는 드롭다운 빈 항목 선택 (commit 4cdc511)
- (css/style.css, js/index.js) 비활성 필터 화살표 진하게(text-secondary/0.9), WJIRA 컬럼 80→100px(물음표 잘림 해소) (commit 355fa43)
- (detail.html) 티켓 상세 메타 4단 중 '티켓 바로가기' 헤더 텍스트 삭제(빈 label로 컬럼 폭·높이 유지), 4단 구성 유지 (commit daaf7e4)
- (js/index.js) 섹션 자동접힘을 원본 개수 기준으로 판정 → 필터/검색 결과가 0개여도 그룹은 열린 채 유지, 애초에 티켓이 없을 때만 접힘 (commit 226d5b5)
- (js/index.js, js/i18n.js) 필터 드롭다운 첫 항목에 '전체' 라벨 표시(빈칸 → 필터 해제 명시), i18n filter_all 키 추가(ko/jp/en/vi) (commit daeb1ec)

## 2026-07-02 JST
- (CLAUDE.md) 파일 수정 시 필수 규칙 간소화: index.html 특별 갱신 폐지, 수정히스토리.md/숙제.md 세션 종료 시 일괄 갱신으로 변경, 커밋&푸시는 코드 변경 즉시 유지 (commit 1f0305d)
- (js/api.js, js/detail.js, js/index.js, css/style.css, js/i18n.js) 각 파일 상단 누적 수정 주석을 최신 1줄로 정리, 나머지 30줄을 수정히스토리.md로 이동 (commit 642d1e6)
- 머지 충돌 해결 (CLAUDE.md, detail.html, index.html, 수정히스토리.md, 숙제.md 5개 파일) + git remote wwpay-check 제거 (commit 38c8fff)

## 2026-06-30 JST
- index.html / detail.html / versions.html: 파비콘 추가 (`<link rel="icon" href="marusys-ci.png">`)
- (js/index.js) 잠긴 항목은 목록 인라인 컨트롤(순서/담당자/상태/결과/WJIRA/핸들)도 변경 불가 처리
- (js/index.js) 이슈명도 커스텀 툴팁(data-tip)으로 전환
- (js/index.js) 클립/자물쇠 툴팁을 요소 위쪽 커스텀 툴팁(data-tip)으로 변경 (커서가 글씨 안 가림)
- (css/style.css) 툴팁을 native title 풍(옅은 노랑+테두리)으로, 행 전체 hover 강조로 통일
- (css/style.css) 커스텀 툴팁(.app-tooltip) 추가: 요소 위쪽 표시로 커서가 글씨 안 가림
- (css/style.css) 자물쇠 아이콘 위 커서 화살표 유지 (cursor:default, user-select:none)

## 2026-06-29 (잠금·자동갱신 — HEAD)
- 드래그 핸들 기능 복구: setupDragDrop / drag-handle-cell td / priority-num span (커밋 8030d47 기반)
- js/index.js: buildRow에 group 파라미터 추가, orderCell select→span, draggable-row/drag-handle-cell 추가
- js/index.js: setupDragDrop 함수 추가 (핸들 드래그 → DOM 순서 → priority 재계산 → GAS 저장)
- css/style.css: .drag-handle-cell / .drag-handle / .draggable-row.dragging / .priority-num 스타일 복구
- 티켓 편집 잠금 기능 추가 (locked_at 컬럼 Q, lockTicket/unlockTicket/checkLock)
- Code.gs: COL.LOCKED_AT(16), rowToObj, doPost 라우터, 3개 함수 추가
- js/api.js: lockTicket / unlockTicket / checkLock wrapper 추가
- js/detail.js: loadTicket에서 잠금 시도, 이미 잠긴 경우 읽기전용 모드, 저장/취소/삭제/beforeunload 시 잠금 해제
- detail.html: 편집 잠금 배너(#lock-banner) 추가
- js/index.js: locked_at 30분 이내 시 🔒 아이콘 표시
- css/style.css: .lock-banner / .lock-icon 스타일 추가
- cascadeShift 함수 누락 버그 수정 (빈칸은 밀지 않음, 연속된 번호만 cascade)
- (js/detail.js) 로딩 속도 개선: lockTicket+getTickets 병렬, 이탈 시 unlock을 sendBeacon(비차단)으로
- (js/detail.js) 편집 잠금: 읽기전용 모드 → 팝업(확인) 후 목록 이동 방식으로 변경
- (js/index.js) 클립(첨부 대표 파일명)·자물쇠(편집중) title 툴팁 추가
- (js/index.js) 잠긴 항목 클릭 시 즉시 팝업(캐시 판단, 상세 진입 생략), 내가 푼 항목 자물쇠 억제
- (js/index.js) 편집 잠금 폴링 추가 (20초 주기, 아이콘만 갱신, 비활성 탭 스킵)
- (js/index.js) 드래그 드롭 위치 녹색 인디케이터 줄 추가 (drop-above/drop-below)
- (js/index.js) 드래그 핸들 실제 동작 수정 (mousedown 시 draggable 토글, tr 고정 draggable 제거)
- (js/index.js) 실시순서 드롭다운 복구(드래그 핸들 병행), 그룹 이동 시 priority 초기화로 중복 방지
- (js/index.js) 담당자/진행상태 컬럼 폭 확대(110/120px)로 셀렉트 간 간격 확보
- (js/index.js) 기본 탭 최신 버전으로 변경, 컬럼 폭 조정 및 가로 스크롤 제거
- (js/index.js) COL_WIDTHS 컬럼 폭 조정 (핸들 추가 후 레이아웃 균형)
- (js/index.js) 진행중 상태 변경 시 row-active 클래스 동적 반영
- (js/index.js) 진행중 행 강조 스타일 추가 (연노랑 배경 + 앰버 왼쪽 보더)
- (css/style.css) 인라인 셀렉트 모양을 등록폼 셀렉트와 통일 (사각 6px + 테두리), 색상/효과 유지
- (css/style.css) 진행중 행은 셀 개별 hover 제거, 행 전체 진한 노랑 hover만 적용
- (css/style.css) 드래그 드롭 위치 녹색 인디케이터(.drop-above/.drop-below) 추가
- (css/style.css) 담당자/진행상태 컬럼 폭 확대(110/120px)로 셀렉트 간 간격 확보, min-width 940px
- (css/style.css) 진행상태 셀 status-cell 클래스화, 왼쪽 패딩 14px로 담당자와 간격 확보
- (css/style.css) 담당자/진행상태 셀렉트 최소 폭 및 여백 확보
- (css/style.css) 기본 탭 최신 버전으로 변경, 컬럼 폭 조정 및 가로 스크롤 제거 (min-width 870px)
- (css/style.css) 자물쇠 아이콘 절대위치+티켓번호칸 좌측 여백 고정으로 행 정렬 유지
- (css/style.css) 편집 잠금: .lock-banner 제거(팝업 차단 방식), .lock-icon 유지
- (css/style.css) 진행중 셀렉트 보색 강조 스타일 적용
- (css/style.css) 진행중 행 강조 스타일 추가 (연노랑 배경 + 앰버 왼쪽 보더)

## 2026-06-29 JST (메타 4단·스크롤·핸들 — origin/master)
- detail.html: 상단 메타 3단 → 4단 (티켓번호 | 바로가기 | 등록날짜 | WJIRA 결과기재)
- detail.html: 바로가기 버튼을 ticket-id-row에서 분리 → 독립 form-group 컬럼으로 이동
- css/style.css: .meta-grid grid-template-columns 1fr 1fr 1fr → 1fr 1fr 1fr 1fr, gap 32→24px
- detail.html: btn-wjira-link에 btn-xs + btn-link-compact 클래스 추가 (소형 버튼)
- css/style.css: .ticket-id-row flex-wrap 제거 → 단일 행 고정, overflow:hidden으로 컬럼 이탈 차단
- css/style.css: .ticket-id-wrap min-width: 0 (입력란이 버튼에 밀려 자연스럽게 줄어들도록)
- css/style.css: .btn-link-compact 신규 — flex-shrink:0, font-size:11px, padding:3px 8px
- js/detail.js: initNewMode — 바로가기 버튼 번호 입력 전에도 항상 표시, href만 input 따라 업데이트
- detail.html: btn-wjira-link 복원 — ticket-id-row 안에 `<a>` 버튼으로 추가 (기본 hidden)
- js/detail.js: fillForm — 수정 모드에서 버튼 숨김 (ticket-id-static 링크로 대체)

## 2026-06-29 JST (가로스크롤 동작 정정)
- 요구사항 재확인: 가로스크롤을 무조건 없애는 게 아니라, 브라우저가 좁아 컬럼 내용
  (특히 이슈명)을 제대로 못 보일 때만 스크롤이 생겨야 함
- 직전 수정에서 min-width를 완전히 제거 → 좁은 화면에서 이슈명(auto 컬럼)이 0px로 수축해 사라짐
- css/style.css: .ticket-table min-width: 950px 복원 (고정 컬럼 720px + 이슈명 최소 ~230px)
- css/style.css: @media 900px/768px 의 min-width 오버라이드(720/680) 제거 (기본 950 유지)

## 2026-06-29 JST (가로스크롤 완전 수정)
- css/style.css: .ticket-table td 에 overflow:hidden 추가 (셀 내용 클리핑)
- css/style.css: .inline-select 에 max-width:100% 추가
- css/style.css: .assignee-select min-width 80px → 0 (컬럼 초과 제거)
- css/style.css: .inline-input 에 max-width:100% 추가

## 2026-06-29 JST (가로스크롤 수정 1차)
- css/style.css: .ticket-table min-width: 800px 제거
  - 좁은 화면 보호: @media (max-width:900px) { min-width:720px }, @media (max-width:768px) { min-width:680px }

## 2026-06-29 JST (드래그 핸들 컬럼 추가)
- js/index.js: COL_WIDTHS에 36px 핸들 컬럼 추가, buildHeaderHtml 빈 `<th>` 추가
- js/index.js: buildRow — 활성 행에 .drag-handle-cell + ⠿ 핸들 추가 (done/hold는 빈 셀)
- js/index.js: setupDragAndDrop() — mousedown 핸들 감지, dragstart/dragover/drop 이벤트, DOM 순서 변경
- js/index.js: savePriorityOrder() — DOM 순서 기준 1,2,3... 재계산, 메모리 캐시 반영, GAS 순차 저장
- css/style.css: .drag-handle-cell, .drag-handle, tr.dragging, drag-over-top/bottom 인디케이터 추가

## 2026-06-28 20:00 JST
- js/index.js: WJIRA 헤더 레이블 'WJIRA 결과기재' → 'WJIRA', wrap()에 iconHtml 파라미터 추가
- js/index.js: WJIRA 헤더에 빨간 물음표 아이콘(.th-help-icon) 추가, title="WJIRA 결과 기재" 툴팁
- css/style.css: .th-row (가로 정렬), .th-help-icon (빨간 원형 아이콘) 스타일 추가

## 2026-06-28 19:30 JST
- js/index.js: buildHeaderHtml wrap 함수 재설계 — 컬럼명 항상 표시, 활성 필터는 하단 뱃지(× 포함)로 분리
- js/index.js: th-filter-clear 클릭 이벤트 위임 추가 (× 버튼으로 필터 해제)
- css/style.css: .th-content / .th-filter-badge / .th-badge-text / .th-filter-clear 스타일 추가

## 2026-06-28 19:00 JST
- css/style.css: --ww-mid/#dbeafe, --mvn-mid/#dcfce7, --done-mid/#e5e7eb, --hold-mid/#d1d5db 변수 추가
- css/style.css: 그룹 헤더 배경 light → mid (한 단계 진하게)
- css/style.css: .section-ww/mvn/done/hold .ticket-table th 섹션별 색상 오버라이드 추가
- css/style.css: .th-filter-label, ::after color → inherit (th 색상 상속)
- js/i18n.js: section_hold 4개 언어 '/ N/A' → '& N/A'
- index.html: 폴백 텍스트 '보류 / N/A' → '보류 & N/A'

## 2026-06-28 18:30 JST
- [롤백] DnD 실시순서 기능 전체 제거
- js/index.js → edfabff 상태로 복구 (setupDragDrop 등 DnD 코드 삭제)
- css/style.css: .drag-handle / .draggable-row / .priority-num 블록 제거 (헤더 스타일 개선은 유지)
- index.html: head 주석 갱신

## 2026-06-28 18:00 JST
- css/style.css: --text-secondary: #6b7280 변수 추가 (--text-muted보다 한 단계 진함)
- css/style.css: .ticket-table th — font-size 11px, font-weight 500, color var(--text-secondary), letter-spacing 0.04em, padding 6px 14px
- css/style.css: .th-filter-label — 동일 스타일 적용 (11px, 500, var(--text-secondary))

## 2026-06-28 17:30 JST
- [롤백] 드래그 핸들 전용 컬럼 추가 커밋(8030d47) 되돌림
- index.html / css/style.css / js/index.js → 9bfc4ea 상태로 복구

## 2026-06-28 17:00 JST
- detail.html: btn-wjira-link 버튼 완전 제거 (신규/수정 모드 모두)
- css/style.css: .ticket-id-wrap에 flex:1 추가 → 신규 등록 시 티켓번호 칸이 1fr 전체를 채움
- js/detail.js: initNewMode linkBtn 참조 제거, fillForm btn-wjira-link 숨김 코드 제거
- js/i18n.js: 베트남어 placeholder_check_content 'Nhập nội dung kiểm tra' → 'Nhập kết quả kiểm tra' (누락 수정)

## 2026-06-28 16:00 JST
- css/style.css: --text-muted / --surface-1 변수 추가, 테이블 헤더(th) uppercase + letter-spacing 0.05em + 색/배경 개선
- css/style.css: .drag-handle / .draggable-row.dragging / .priority-num 스타일 추가
- js/index.js: 실시순서 select → DnD 핸들(⠿)+숫자 표시로 교체
- js/index.js: setupDragDrop 함수 추가 — 드래그 완료 시 priority 재계산 후 GAS 저장
- js/index.js: handleInlineChange에서 priority select 처리 블록 제거
- js/detail.js: 실시순서 readonly input으로 변경, handlePriorityChange 제거, pendingCascadeUpdates 제거
- detail.html: priority select → readonly input 교체
- js/i18n.js: WJIRA 바로가기 → 티켓 바로 가기 (한국어) / チケットへ (일본어) / Open Ticket (영어) / Đến vé (베트남어)

## 2026-06-28 15:20 JST
- js/detail.js: 수정(상세) 모드에서 WJIRA 바로가기 버튼 숨김 (티켓번호 자체가 링크로 표시되므로 중복 불필요)
- js/i18n.js: label_check_content / placeholder_check_content — 4개 언어 모두 "확인내용" → "확인결과" 로 변경
- detail.html: fallback 텍스트도 "확인결과" 로 수정

## 2026-06-28 15:00 JST
- detail.html: 전체 레이아웃 리디자인 (단일 카드, 메타 3단, 구분선, 3단 그리드, 하단 버튼 제거)
- css/style.css: :root에 --bg-accent / --text-accent / --border-accent 추가
- css/style.css: detail 페이지 CSS 전면 재작성 (.detail-card, .meta-grid, .detail-3col, .form-divider, .version-badge, .version-move-header)
- css/style.css: 반응형 — .meta-grid / .detail-3col 1fr으로 전환
- js/detail.js: updateCurrentVersionLabel → version-move-badge 엘리먼트 참조로 변경

## 2026-06-28 14:20 JST
- detail.html: Row1 `form-row-3` 래퍼 제거 → 3개 form-group을 outer `.ticket-form` 3단 그리드에 직접 배치
- css/style.css: `.form-row-3` 규칙 삭제 (사용처 없어짐)
- 효과: 티켓번호/등록날짜/WJIRA결과기재 컬럼이 아래 확인버전/진행정보/버전이동 컬럼과 같은 그리드 공유 → 자동 정렬

## 2026-06-28 14:10 JST
- js/i18n.js: 바로가기 버튼 레이블 '바로가기' → 'WJIRA 바로가기' (한국어)

## 2026-06-28 14:00 JST
- 실시순서 로직 전면 재작성 (Rule 1~5 적용)
- js/i18n.js: `cascadeShift` / `computeCascadeUpdates` 공용 함수 추가 (연속된 번호만 밀기, 빈칸에서 중지)
- Code.gs: `renumberActiveGroup` 함수 및 모든 호출부 제거 (updateTicket, deleteTicket, moveTicket)
- Code.gs: `moveTicket` — 활성 티켓 이동 시 priority `9999` → `''` 초기화 (사용자가 새 버전에서 직접 지정)
- Code.gs: `updateTicket` / `deleteTicket` — `SpreadsheetApp.flush()` + renumber 호출 제거
- js/index.js: Rule 4 — cascade 대상을 같은 그룹(WW/MVN)+같은 버전으로 제한, 연속된 번호만 밀기
- js/detail.js: Rule 1 — 신규 등록 시 같은 그룹+버전 기준 max+1 계산, 담당자 변경 시 실시순서 자동 재계산
- js/detail.js: Rule 4 — cascade 대상을 같은 그룹+버전으로 제한, 저장 시 cascade 티켓 함께 GAS에 반영
- js/detail.js: `pendingCascadeUpdates` 모듈 변수 추가, `getSuggestedPriority` 함수 추가
- GAS 재배포 필요

## v0.3.0
- 실시순서 5개 규칙 전면 재작성: 그룹+버전 독립, 빈칸 gap 보존, cascade 연속 번호만 밀기

## 2026-06-28 12:30 JST
- js/detail.js: loadTicket() — `data.activeWW` → `cachedAllTickets.activeWW` (data is not defined 오류 수정)
- Code.gs: updateTicket() — setValues 후 SpreadsheetApp.flush() 추가 → renumberActiveGroup가 이전 상태를 읽는 버그 수정
- Code.gs: deleteTicket() — deleteRow 후 SpreadsheetApp.flush() 추가
- Code.gs: moveTicket() — setValue 후 SpreadsheetApp.flush() 추가
- 재번호 버그 원인: GAS가 setValues를 버퍼링 → renumber 시 이동된 티켓이 아직 활성으로 읽힘 → priority 슬롯 점유 → 나머지 활성 티켓 번호에 공백 발생
- GAS 재배포 필요

## 2026-06-28 12:00 JST
- css/style.css: `.ticket-table th` — `overflow: hidden` 추가 (항상 가로 스크롤 버그 수정)
  - 원인: `th-filter-wrap { display: inline-flex; white-space: nowrap }` + WJIRA 열 "WJIRA 결과기재" 레이블이 100px 컬럼 내용폭(76px)보다 넓어 테이블 우측 밖으로 overflow → .table-scroll scroll width 증가 → 항상 스크롤
- css/style.css: `.ticket-table` — `min-width: 750px` → `800px`
- js/index.js: WJIRA 헤더 기본 레이블 "WJIRA 결과기재" → "W.결과기재" (컬럼 폭 내 수용)

## 2026-06-28 11:30 JST
- css/style.css: `.main-content` — `max-width: 1280px; margin: 0 auto;` 제거, `padding: 20px 24px` → `16px 8px`
- css/style.css: `.ticket-table` — `min-width: 950px` → `750px` (고정열 684px + 최소여백)
- css/style.css: `@media 768px` — `.main-content` padding 오버라이드 제거 (기본값이 이미 소형)
- 효과: 사이드탭+메인이 브라우저 전체 폭 채움, 넓을 때 스크롤 없음, 좁을 때(750px 미만)만 스크롤

## 2026-06-28 11:00 JST
- css/style.css: `.ticket-table` min-width 680px → 950px, 가로 스크롤 (`.table-scroll`은 기존에 overflow-x:auto 적용되어 있었음)
- js/index.js: COL_WIDTHS 각 컬럼 최소 너비 조정 (티켓번호 110px, 이슈명 auto≥266px, 담당자 90px, 진행상태 100px, 결과 70px, WJIRA 100px)

## 2026-06-28 10:30 JST
- index.html: 로딩 오버레이 클래스 `loading-overlay` → `detail-loading-overlay`로 통일, 스피너 + "로딩 중..." 텍스트 추가
- versions.html: 로딩 오버레이 텍스트 "불러오는 중..." → "로딩 중..." + data-i18n 속성 추가

## 2026-06-28 10:00 JST
- 로딩 오버레이 즉시 표시: index.html/detail.html/versions.html 각 `<body>` 상단 오버레이를 `display:flex`로 변경하여 JS 로드 전 화면 즉시 덮기
- GAS doGet 속도 개선: versions 시트도 함께 읽어 `{ success, data, versions }` 형식으로 반환
- API 호출 2회→1회 감소: `getTickets()` 반환값에 versions 포함, `getVersions()` 별도 호출 제거
- index.js: `loadVersions()` 함수 제거, `loadTickets()` 내에서 versions 처리 통합
- detail.js: `initNewMode()` 로딩 오버레이 완료 시 자동 해제, API 호출 단일화
- versions.js: `Promise.all([getVersions(), getTickets()])` → `getTickets()` 단일 호출로 변경

## 2026-06-28 (이전 세션)
- detail.html/js: 저장 버튼 2개로 분리(저장 후 목록으로/저장 후 계속 등록), 복제 버튼·함수 전체 제거
- versions.html/js/css: 드래그앤드롭 순서 변경(원래대로/순서저장), 헤더 ↕ 정렬, 드롭 인디케이터, 하이라이트
- versions.html/js: 테이블 헤더 정렬 기능(버전명/티켓수/생성일 ▲▼ 토글); 로딩 오버레이 텍스트 추가
- index.html: 사이드바 버튼 텍스트 "새 버전 추가" → "버전 관리"

## 2026-06-27
- detail.html/js/css: 3단 그리드 레이아웃 (확인버전/진행정보/버전이동)
- detail.js: 버전이동 레이블 현재 버전명 표시, 신규 등록 시 최신 버전 자동 선택
- 사이드탭 폴더 스타일 디자인 적용
- 버전 관리 페이지 추가: versions.html + js/versions.js
- GAS: updateVersion / deleteVersion 추가
- 버전 탭 기능: versions 시트, version_id 컬럼(P), getVersions/addVersion/moveTicket

## 2026-06-26
- 프론트엔드 초기 구축: index.html, detail.html, css, js 전체
- Code.gs 초기 구축: doGet, addTicket, updateTicket, fetchJira, uploadFile
