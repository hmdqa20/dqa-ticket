# 숙제 (작업 인수인계)

> 집 ↔ 회사 기기를 오가며 작업을 이어가기 위한 파일.
> 다른 기기에서 새 Claude 세션을 시작하면 **이 파일을 먼저 읽고** 이어서 작업할 것.
> ⚠️ 세션 시작 시 **반드시 `git pull` 먼저** (회사↔집 누락 방지). 그 다음 이 파일 읽기.
> 최종 갱신: 2026-07-06 JST

---

## 0. ⚠️ 커밋/푸시 규칙 (2026-07-06 변경 — 반드시 지킬 것)

- **파일 수정 시 커밋까지만 자동 실행.** `git push`는 **사용자가 "push해줘" 등으로 명시적으로 요청할 때만** 실행한다.
  (CLAUDE.md의 "커밋 & 푸시 즉시" 마무리 규칙보다 이 규칙이 우선 — 커밋은 즉시, 푸시는 요청 시)
- **Code.gs가 포함된 변경을 push할 때는, 그 시점에 "GAS 배포 관리 → 기존 배포 편집 → 새 버전"으로 재배포도 같이 해야 한다는 점을 사용자에게 함께 안내**할 것.

---

## 1. 현재 상태

이번 세션(2026-07-06) 모든 코드 변경 완료·커밋·**푸시 완료**.
master 최신 커밋: `1058ea2`. 작업 트리 깨끗, 전부 푸시됨.

### 이번 세션(2026-07-06) 변경 요약
1. 드래그 재정렬 시 잠긴(locked) 항목도 재번호 계산에 포함 → 실시순서 중복 버그 수정
2. 드래그로 번호구역 끝 경계(마지막 번호 ↔ 첫 빈칸 사이)에 놓을 때 번호가 사라지던 버그 수정 (3구간 판정으로 세분화)
3. 드래그 핸들 아이콘 ⠿ → 가로선 3개 SVG로 교체, 색상 진하게
4. "WW" 섹션 표시명 → "DQA" (4개 언어 통일, 내부 키 activeWW는 불변)
5. 편집 잠금 타임아웃 30분 → 5분 단축
6. 편집 잠금 heartbeat 추가 (2분 주기 LOCKED_AT 갱신, 이탈 시 자연 만료 5분)
7. 순수 영어(비일본어) 이슈명도 번역 + "번역 / 원문" 슬래시 병기 표시
8. backfillTitleTranslationsForce 레이트리밋 오류 수정 (throttleMs 인자, 호출 간격 1초)
9. WJIRA 물음표 아이콘 툴팁이 커서에 가려 안 보이던 문제 수정 (native title → data-tip)

**⚠️ GAS 재배포 완료 여부 확인 필요**: 5·6·8번은 Code.gs 변경분. 세션 중 한 차례 사용자가 직접 복붙 재배포했다고 확인됨(5·6번 시점). **8번(레이트리밋 수정)은 그 이후 커밋**이라 재배포 여부 미확인 — 다음 세션 시작 시 GAS 에디터의 Code.gs 내용이 최신(`1058ea2` 기준)과 일치하는지, 특히 `backfillTitleTranslationsForce`의 `THROTTLE_MS=1000` 반영 여부를 확인할 것.

---

## 2. 확정된 결정사항 (되돌리지 말 것)

### 실시순서(priority) 드래그 3구간 판정 — `dragend`(js/index.js)
드롭 후 DOM에서 드래그 항목의 앞/뒤 이웃으로 판정:
- **A. 바로 뒤가 번호항목** → 번호 항목들 DOM 순서로 1..n 전체 재번호(cascade), 빈칸은 그대로
- **B. 바로 뒤는 번호 아님 + 바로 앞은 번호항목(번호구역 끝 경계)** → 드래그 항목만 `max+1`(자기 제외 최댓값) 할당, 원래 자리는 갭 유지, **나머지 항목 불변**
- **C. 양쪽 다 번호 아님(빈칸구역)** → 드래그 항목 번호 삭제(갭 유지), 나머지 불변
- 잠긴 항목(`.locked-row`)도 `rows` 수집에 포함되어 재번호 계산 대상 — 단, 직접 드래그는 여전히 불가(draggable-row 미부여). "핸들 잠금(직접 이동 불가)"과 "시스템에 의한 순서 밀림"은 별개 개념.
- 실시순서 정렬: 번호 있는 항목 오름차순 → 빈칸끼리는 티켓번호 오름차순(`sortByPriority`+`ticketNo`)

### 편집 잠금 (locked_at) — 타임아웃 5분 + heartbeat
- **타임아웃 5분**: `js/index.js`의 `LOCK_EXPIRE_MS`(표시용) + `Code.gs`의 `lockTicket()`/`checkLock()`(실제 강제, 두 곳 값 반드시 일치) — 3곳 모두 5분으로 통일됨.
- **heartbeat(2분 주기)**: detail.html 편집 중 `js/detail.js`의 `startHeartbeat(rowId)`가 2분마다 `heartbeat(rowId)` API 호출 → `Code.gs`의 신규 `heartbeat(e)` 함수가 `LOCKED_AT`만 현재 시각으로 갱신(다른 컬럼 불변, LockService 보호). `LOCKED_AT`이 이미 비어있으면(타 세션이 새로 잠금 획득) 갱신 안 하고 무시(`relocked:false`) — 죽은 세션이 남의 잠금 되살리는 사고 방지.
- **정지(`stopHeartbeat`)**는 `releaseLockNow()`/`handleDelete()`/`handleLockBeforeUnload()` 모든 이탈 경로에 연결 — 떠난 세션이 계속 갱신하는 버그 방지.
- **"편집중입니다" 상태 = locked_at 그 자체**, 별도 상태값 없음(과거 언어전환 버그와는 무관한 별개 메커니즘, 이미 분리되어 있음 — detail.js:49 주석 참고).
- 자동저장/무동작(inactivity) 감지 타이머는 **없음, 이번 범위에도 포함 안 함**.

### 이슈명 다국어 번역 — `buildTranslatedTitle(title, targetLang, throttleMs)` (Code.gs)
- **일본어 run 있음** → 그 구간만 개별 번역(기존 방식 그대로, 변경 없음)
- **일본어 run 없음**:
  - 원문이 이미 대상 언어 문자 위주(한글 `[가-힣]` / 베트남어 전용문자) → 원문 그대로(번역 호출 안 함)
  - 그 외(순수 영어 등) → `LanguageApp.translate(title, '', targetLang)`로 **전체 통째 번역**
- `title_ko`/`title_vi` 컬럼엔 순수 번역 결과만 저장. 프론트(`js/index.js`, `js/detail.js`)에서 **일본어 미포함 + 실제 번역됨**이면 `"번역 / 원문"` 슬래시 병기로 표시(목적: "이 티켓이 일본어 없이 등록됐다"는 사실 자체를 알리기 위함). 일본어 포함 시엔 기존처럼 번역 결과만 표시.
- **"i" 아이콘 클릭 시 팝오버는 항상 순수 원문만**(`data-orig`가 `ticket.title` 직접 참조 — 슬래시 병기 문자열이 아님).
- `throttleMs` 인자: 지정 시 `translate` 호출 직후마다 `Utilities.sleep(throttleMs)`. **`addTicket`/`updateTicket` 등 실시간 경로는 인자 안 넘김(기존과 동일, 지연 없음)**. `backfillTitleTranslationsForce`만 `THROTTLE_MS=1000`으로 넘겨 레이트리밋 회피.
- 기존 티켓 소급 적용하려면 GAS 에디터에서 **`backfillTitleTranslationsForce()`** 수동 실행 필요 (완료 로그에 처리/건너뜀/오류 건수 + 실측 소요시간 출력됨). ⚠️ GAS 실행 6분 제한 있음 — 티켓 많으면 중간에 끊길 수 있음(청크 분할은 미구현).

### 헤더 필터 UI / 툴팁
- 헤더 필터 아이콘: 비활성=얇은 ▼, 활성=깔때기 SVG+인디고(`--filter-active`)+pill 배경. 필터 해제는 드롭다운 첫 항목 "전체"(`filter_all` i18n 키) 선택.
- 섹션 자동접힘 판정은 **원본 개수** 기준 — 필터 결과 0개여도 그룹 열림 유지.
- **모든 커스텀 툴팁은 `data-tip` + `.app-tooltip`(body 부착, 요소 위쪽 표시) 방식으로 통일**. native `title` 속성은 커서에 가려 안 보이는 문제가 있어 지양 — WJIRA 물음표 아이콘도 이번에 `data-tip`으로 전환 완료.

### 파비콘 / 표시명
- 파비콘: `favicon_32x32.png`(마루시스 공식, 3개 HTML 공통).
- "WW" 섹션 표시명 → **"DQA"**로 변경(i18n `section_activeWW` 4개 언어). 내부 키 `activeWW`/클래스/변수명은 전부 불변.

### GAS 배포 규칙
- **"새 배포" 금지(URL 바뀜)** → **배포 관리 → 기존 배포 편집 → 새 버전**.
- GAS URL(회사 계정): `.../AKfycbwIgVHDvVDcS1A6zyopK9NebKD0e2qdWDhLTaK3gR_DY5dQlvE5dLUiv_i89_-TW3QJ7A/exec`

---

## 3. 남은 할 일 (TODO)

1. **GAS 재배포 상태 확인** ← 최우선
   - `backfillTitleTranslationsForce`(THROTTLE_MS 추가분, commit 5c859cd)이 재배포됐는지 확인
   - 재배포 안 됐다면: 배포 관리 → 기존 배포 편집 → 새 버전
2. **자물쇠 항목 cascade 규칙 결정 & 구현** (오래 보류 중)
   - 드롭다운으로 중복 번호 입력 시 `cascadeShift`가 자물쇠(다른 사용자 편집 중) 항목의 번호도 바꿈.
   - 옵션 A: cascade에서 자물쇠 건너뜀(번호 충돌 가능) / 옵션 B: 현행 유지(자물쇠 번호도 변경). **사용자 결정 필요.**
3. **목록 컬럼 필터 체크박스 멀티셀렉트** (기존 백로그, 오래 보류)
   - 헤더 아이콘 클릭 시 체크박스 멀티셀렉트 드롭다운, 검색창, 전체선택 등.
4. **backfillTitleTranslationsForce 실행 필요 여부 확인**
   - 기존 티켓 중 순수 영어 제목이 있다면, 새 번역 로직(이번 세션 2a541b4) 소급 적용을 위해 GAS 에디터에서 수동 실행 필요. 아직 실행 안 했다면 다음 세션에서 실행 여부 확인.

---

## 4. 주의·보류 사항

- **세션 시작 시 `git pull` 필수** (회사↔집 누락 사고 방지).
- **커밋만 자동, 푸시는 명시적 요청 시에만** (0번 항목 참고). Code.gs 포함 push 시 재배포 안내 필수.
- **GAS 재배포는 기존 배포 편집 → 새 버전** (새 배포 만들면 URL 바뀌어 앱과 불일치 → 기능 조용히 죽음).
- PowerShell here-string(`@'...'@`)은 `if(){}` 중첩 블록 안에서 깨질 수 있음 → 멀티라인 커밋 메시지는 Bash 툴로 커밋하는 게 안전.
- **Code.gs Edit 시 CRLF로 인해 큰 블록 old_string 매칭이 실패하는 경우가 있음** — 실패하면 작은 단위(한 줄~두 줄)로 쪼개서 재시도할 것.
- Code.gs는 `.gs` 확장자라 `node --check` 불가 → `/tmp`에 `.js` 복사본을 만들어 문법 검증하는 방식 사용 중.
- GAS 실행 시간 제한(약 6분) — `backfillTitleTranslationsForce`가 티켓 수 많으면 중간에 끊길 수 있음(청크 분할 미구현, 필요시 별도 요청).
